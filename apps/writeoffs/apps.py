from django.apps import AppConfig


class WriteOffsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.writeoffs"
    verbose_name = "Списания (брак / потеря / утилизация)"
