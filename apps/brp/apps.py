from django.apps import AppConfig


class BrpConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.brp"
    verbose_name = "BRP-каталог (справочник дилерского прайса)"
