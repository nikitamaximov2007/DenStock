# Current handoff

Задача завершена.

- Task: данные и оформление таможенного Excel (`/inventory/actions/export/`).
- Branch: `fix/customs-export-formatting` (в main не пушилось).
- Что было не так и как исправлено:
  - **Съезжала последняя строка.** Экспорт никогда не задавал выравнивание, а
    в шаблоне у части строк товарного диапазона жёстко проставлена высота
    (15.0 / 18.0). Последняя фактическая строка попадала на такую строку и
    визуально отличалась. Теперь `_center_data_row()` для каждой записанной
    строки ставит собственный `Alignment(center, center, wrap_text)` в каждую
    ячейку A..M (свой объект на ячейку, без общего mutable-стиля) и сбрасывает
    `row_dimensions[row].height = None` (авто-высота).
  - **Страна.** Было `(customs.country_of_origin or "КАНАДА").upper()` для BRP
    и пусто для Polaris. Стало: константа `CUSTOMS_COUNTRY = "CANADA"`
    безусловно для всех строк.
  - **Стоимость за шт.** Было `retail_price_usd` через
    `find_brp_price_source` / `find_polaris_price_source`. Стало: оптовая цена
    прайса — `_brp_wholesale_usd()` / `_polaris_wholesale_usd()`, оба берут
    `wholesale_price_usd`; replacement (BRP) и superseded (Polaris) служат
    ТОЛЬКО источником цены и не подменяют артикул в колонке B. Курс
    (`ValuationSettings.current_usd_rate`) и наценки не применяются. Нет
    оптовой — ячейка пустая, клиентская цена не подставляется.
  - **Область применения.** Было `(customs.application_area or "МОТО
    ЗАПЧАСТИ").upper()`. Стало `resolve_customs_application(part)` — только по
    фактической применимости каталога `PartCompatibility -> VehicleModel ->
    VehicleMake -> VehicleType`. Категории: СНЕГОХОД, КВАДРОЦИКЛ, ГИДРОЦИКЛ,
    КАТЕР / ЛОДКА, АВТОМОБИЛЬ; несколько категорий -> УНИВЕРСАЛЬНЫЕ ЗАПЧАСТИ;
    нет данных или только мотоциклы -> пустая ячейка. Ручное значение в
    `PartCustomsInfo.application_area` побеждает, кроме легаси-дефолта
    «МОТО ЗАПЧАСТИ» — он считается «не заполнено». Модель и миграции не
    трогались.
  - **Заготовка шаблона.** Шаблон предзаполнен BRP/CANADA/СНЕГОХОД и формулами
    примерно до 149-й строки; экспорт чистил только колонки `ABCDGHJK` строки
    10. Теперь очищаются значения строк 10..149 по колонкам A..M (merged-ячейки
    пропускаются: `MergedCell.value` read-only, `F150:H150`). Стили, границы,
    заливка, ширины колонок сохраняются; формулы пишутся только в фактические
    строки, поэтому пустые строки больше не показывают ложные `0,00`.
- Строки 1-9 (шапка и подсказки брокера) не трогаются: `M7:M9` шаблона
  содержит подсказку «МОТО ЗАПЧАСТИ» — это текст шаблона, а не выгруженная
  позиция. Тест закрепляет и то, и другое.
- Контракт сохранён: лист «Лист1», данные с 10-й строки, колонки A..M,
  формулы `=J{r}*G{r}` / `=K{r}*J{r}` со ссылкой на свою строку, exact identity
  (BRP `material_no` / Polaris `part_number` / warehouse snapshot),
  группировка по (производитель, номер), отменённые в таможню не попадают,
  экспорт read-only.
- Миграций нет. Исторические snapshots, суммы и цены не трогались.
- Тесты: `tests/test_customs_export.py` — 51 (24 новых на выравнивание,
  страну, оптовую цену, область применения, очистку заготовки, формулы).
  Обновлены `tests/test_actions.py` и `tests/test_polaris.py` под новый
  контракт (страна CANADA, оптовая цена, пустая область применения).
